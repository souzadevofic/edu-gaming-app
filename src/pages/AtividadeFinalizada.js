import React from 'react';
import { MensagemFinalizada } from '../components/MensagemFinalizada/MensagemFinalzada';

export default function AtividadeFinalizada( { navigation } ) {
    return(
        <MensagemFinalizada navigation={navigation} />
    )
}
